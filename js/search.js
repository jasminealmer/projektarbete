








/*function searchUserType()
{
  // Declare variables
  var input, filter, table, tr, td, i;
  input = document.getElementById("userTypeInput");
  filter = input.value.toUpperCase();
  table = document.getElementById("usersTable");
  tr = table.getElementsByTagName("tr");

  // Loop through all table rows, and hide those who don't match the search query
  for (i = 0; i < tr.length; i++) {
    td = tr[i].getElementsByTagName("td")[0];
    if (td) {
      if (td.innerHTML.toUpperCase().indexOf(filter) > -1) {
        tr[i].style.display = "";
      } else {
        tr[i].style.display = "none";
      }
    }
  }
}





onkeyup="searchUserType()" placeholder="Sök efter användartyp...">
<input type="text" id="myInput" onkeyup="searchEmail()" placeholder="Sök efter email...">
<input type="text" id="myInput" onkeyup="searchName()" placeholder="Sök efter namn...">
<input type="text" id="myInput" onkeyup="searchPhone()" */
